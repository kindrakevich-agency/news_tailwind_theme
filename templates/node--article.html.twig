{#
/**
 * @file
 * Theme implementation to display an article node.
 *
 * Available variables:
 * - node: The node entity
 * - content: Node content render array
 * - label: The title of the node
 * - url: Direct URL to node
 * - date: Formatted creation date
 * - author_name: Author's name
 * - field_tags: Tags field
 * - field_image: Image field
 * - body: Body field
 */
#}
<article{{ attributes.addClass('max-w-3xl', 'mx-auto', 'px-4', 'sm:px-6', 'lg:px-8', 'py-12') }}>
    <!-- Article Header -->
    <header class="mb-8">
        {% if label %}
            <h1 class="text-3xl font-bold text-gray-900 text-center mb-6">
                {{ label }}
            </h1>
        {% endif %}

        {% if content.field_tags %}
            <div class="flex flex-wrap justify-center gap-2 mb-8">
                {{ content.field_tags }}
            </div>
        {% endif %}
    </header>

    <!-- Featured Image -->
    {% if content.field_image %}
        <div class="mb-8 rounded-2xl overflow-hidden">
            {{ content.field_image }}
        </div>
    {% endif %}

    <!-- Author and Meta Info -->
    <div class="flex items-center justify-between mb-8 pb-6 border-b border-gray-200">
        <div class="flex items-center space-x-3">
            <div class="hidden md:block w-10 h-10 bg-gray-300 rounded-full overflow-hidden">
                {% if author_picture %}
                    {{ author_picture }}
                {% else %}
                    <svg class="w-full h-full text-gray-500" fill="currentColor" viewBox="0 0 24 24">
                        <path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
                    </svg>
                {% endif %}
            </div>
            <div>
                <div class="hidden md:block text-sm font-semibold text-gray-900">{{ author_name }}</div>
                <div class="text-xs text-gray-500">{{ date }}</div>
            </div>
        </div>
        <div class="flex items-center text-sm text-gray-500 space-x-4">
            {% if reading_time %}
                <div class="flex items-center">
                    <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                    </svg>
                    <span>{{ reading_time }}<span class="hidden md:inline"> {% if reading_time == 1 %}{{ 'minute'|t }}{% else %}{{ 'minutes'|t }}{% endif %}</span></span>
                </div>
            {% endif %}
            <div class="flex items-center">
                <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                </svg>
                <span>{{ view_count|default('0') }}<span class="hidden md:inline"> {{ 'views'|t }}</span></span>
            </div>

            {# Share Button with Dropdown #}
            <div class="relative share-dropdown-container">
                <button class="share-button p-2 focus:outline-none" data-url="{{ url }}" data-title="{{ label }}" aria-label="{{ 'Share'|t }}">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8.684 13.342C8.886 12.938 9 12.482 9 12c0-.482-.114-.938-.316-1.342m0 2.684a3 3 0 110-2.684m0 2.684l6.632 3.316m-6.632-6l6.632-3.316m0 0a3 3 0 105.367-2.684 3 3 0 00-5.367 2.684zm0 9.316a3 3 0 105.368 2.684 3 3 0 00-5.368-2.684z"></path>
                    </svg>
                </button>
                <div class="share-dropdown hidden absolute right-0 mt-1 w-48 bg-white dark:bg-gray-800 rounded-md shadow-lg z-10 border border-gray-200 dark:border-gray-700">
                    <a href="#" class="share-facebook block px-4 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700 rounded-t-md" data-url="{{ url }}">
                        <svg class="w-4 h-4 inline-block mr-2" fill="currentColor" viewBox="0 0 24 24">
                            <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                        </svg>
                        {{ 'Facebook'|t }}
                    </a>
                    <a href="#" class="share-telegram block px-4 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-700 rounded-b-md" data-url="{{ url }}">
                        <svg class="w-4 h-4 inline-block mr-2" fill="currentColor" viewBox="0 0 24 24">
                            <path d="M12 0C5.373 0 0 5.373 0 12s5.373 12 12 12 12-5.373 12-12S18.627 0 12 0zm5.894 8.221l-1.97 9.28c-.145.658-.537.818-1.084.508l-3-2.21-1.446 1.394c-.14.18-.357.223-.548.223l.188-2.85 5.18-4.68c.223-.198-.054-.308-.346-.11l-6.4 4.03-2.76-.918c-.6-.187-.612-.6.125-.89l10.782-4.156c.5-.18.943.11.78.89z"/>
                        </svg>
                        {{ 'Telegram'|t }}
                    </a>
                </div>
            </div>

            {# Collect Button #}
            <button class="collect-button p-2 focus:outline-none" data-nid="{{ node.id }}" data-collect-text="{{ 'Collect'|t }}" data-collected-text="{{ 'Collected'|t }}" aria-label="{{ 'Collect'|t }}">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"></path>
                </svg>
            </button>
        </div>
    </div>

    <!-- Article Body -->
    {% if content.body %}
        <div class="prose prose-lg max-w-none">
            {{ content.body }}
        </div>
    {% endif %}

    <!-- Navigation Buttons -->
    <div class="flex items-center justify-center space-x-4 mt-12 pt-8 border-t border-gray-200">
        {% if prev_article %}
            <a href="{{ prev_article.url }}" class="px-6 py-2 text-sm text-gray-700 border border-gray-300 rounded-full hover:bg-gray-50 transition-colors">
                ← {{ 'Previous'|t }}
            </a>
        {% else %}
            <button class="px-6 py-2 text-sm text-gray-400 border border-gray-200 rounded-full cursor-not-allowed" disabled>
                ← {{ 'Previous'|t }}
            </button>
        {% endif %}

        {% if next_article %}
            <a href="{{ next_article.url }}" class="px-6 py-2 text-sm text-white bg-teal-600 rounded-full hover:bg-teal-700 transition-colors">
                {{ 'Next'|t }} →
            </a>
        {% else %}
            <button class="px-6 py-2 text-sm text-gray-400 border border-gray-200 rounded-full cursor-not-allowed" disabled>
                {{ 'Next'|t }} →
            </button>
        {% endif %}
    </div>
</article>

<!-- Related Articles Section -->
{% if related_articles|length > 0 %}
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
        <h2 class="text-3xl font-bold text-gray-900 mb-8">{{ 'You may also be interested'|t }}</h2>

        <div class="masonry">
            {% for article in related_articles %}
                <div class="masonry-item">
                    {{ article }}
                </div>
            {% endfor %}
        </div>
    </div>
{% endif %}
