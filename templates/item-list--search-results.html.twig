{#
/**
 * @file
 * Theme implementation for search results list.
 *
 * Available variables:
 * - items: A list of items.
 * - title: The list title.
 * - list_type: The tag for list element ("ul" or "ol").
 * - wrapper_attributes: HTML attributes for the wrapper.
 * - attributes: HTML attributes for the list element.
 * - empty: A message to display when there are no items.
 * - context: The context for the list (e.g. "search_results").
 */
#}
{% if items %}
  <div class="masonry">
    {% for item in items %}
      <div class="masonry-item">
        {{ item.value }}
      </div>
    {% endfor %}
  </div>
{% elseif empty %}
  <div class="text-center py-12">
    <p class="text-gray-500">{{ empty }}</p>
  </div>
{% endif %}
